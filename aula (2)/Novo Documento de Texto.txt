#aula02

Name Description

accept(2POSIX) -- REDE accept a connection on a socket

access(2POSIX) -- CONTROLE DE ACESSO check access permissions of a file or pathname

bind(2POSIX) -- REDE bind a name to a socket

chdir(2POSIX) -- ARQUIVO change current directory

chmod(2POSIX) -- ARQUIVO change mode of file

chown(2POSIX) -- CONTROLE DE ACESSO change owner and group of a file

chroot(2POSIX) -- ARQUIVO change the root directory

close(2POSIX) -- ARQUIVO close a file descriptor

connect(2POSIX) -- REDE initiate a connection on a socket

dup(2POSIX) -- ARQUIVO duplicate an open file descriptor

dup2(2POSIX) --ARQUIVO See dup(2POSIX)

fchdir(2POSIX) -- ARQUIVO See chdir(2POSIX)

fchmod(2POSIX) -- CONTROLE DE ACESSO See chmod(2POSIX)

fchown(2POSIX) -- CONTROLE DE ACESSO See chown(2POSIX)

fcntl(2POSIX) file control - ARQUIVOS

flock(2POSIX) -- CONTROLE DE ACESSO apply or remove an advisory lock on an open file

fpathconf(2POSIX) -- MEMORIA PRINCIPAL get configurable pathname variables

fstat(2POSIX) -- ARQUIVO See stat(2POSIX)

fstatfs(2POSIX) -- ARQUIVO See statfs(2POSIX)

fsync(2POSIX) -- ARMAZENAMENTO SEC. synchronize a file's in-memory state with that on the physical medium

ftruncate(2POSIX) -- PROCESSO See truncate(2POSIX)

ftruncate(2POSIX) -- ARQUIVO See truncate(2POSIX)

getdirentries(2POSIX) -- ARQUIVO get directory entries in a filesystem|[mdash ]|independent format

getdomainname(2POSIX) get or set the domainname of the current host -- REDE

getfh(2POSIX) -- ARQUIVO get file handle

getfsstat(2POSIX) -- ARQUIVO get list of all mounted filesystems

gethostname(2POSIX) -- REDE See hostname(2POSIX)

getpeername(2POSIX) -- REDE get name of connected peer

getrlimit(2POSIX) -- PROCESSO control maximum system resource consumption

getsockname(2POSIX) -- REDE get socket name

getsockopt(2POSIX) -- REDE get and set options on sockets

gettimeofday(2POSIX) -- PRECESSO get/set date and time

hostname(2POSIX) -- REDE get or set the name of the machine

ioctl(2POSIX) -- SIST. E/S control device

link(2POSIX) -- ARM. INTERNO make a hard file link

listen(2POSIX) -- REDE listen for connections on a socket

lseek(2POSIX) -- SIST. E/S move a read/write file pointer

lstat(2POSIX) -- ARQUIVO See stat(2POSIX)

mkdir(2POSIX) -- ARQUIVO make a directory file

mkfifo(2POSIX) -- ARQUIVO make a fifo file

mknod(2POSIX) -- CONTROLE DE ACESSO make a special file node

mmap(2POSIX) -- PROCESSO map c_actor addresses to a shared memory object

mount(2POSIX) -- SIST. E/S mount or unmount a filesystem

mq_close(2POSIX) -- PROCESSO close a message queue

mq_getattr(2POSIX) -- PROCESSO retrieve message queue attributes

mq_open(2POSIX) -- PROCESSO open a message queue

mq_receive(2POSIX) -- PROCESSO receive a message from a message queue

mq_send(2POSIX) -- PROCESSO send a message to a message queue

mq_setattr(2POSIX) -- PROCESSO set message queue attributes

mq_unlink(2POSIX) --PROCESSO unlink a message queue

munmap(2POSIX) -- PROCESSO unmap a previously mapped address

nfssvc(2POSIX) NFS services -- REDE

open(2POSIX) -- SIST. E/S open for reading or writing

pipe(2POSIX) -- ARM. SEC. create descriptor a pair for interprocess communication

read(2POSIX) -- SIST. E/S read input

readlink(2POSIX) -- SIST. E/S read value of a symbolic link

readv(2POSIX) -- SIST. E/S See read(2POSIX)

recv(2POSIX) -- REDE receive a message from a socket

recvfrom(2POSIX) -- REDE See recv(2POSIX)

recvmsg(2POSIX) -- REDE See recv(2POSIX)

rename(2POSIX) -- ARQUIVO change the name of a file

rmdir(2POSIX) -- PROCESSO remove a directory file

select(2POSIX) -- SIST. E/S synchronous I/O multiplexing

send(2POSIX) -- REDE send a message from a socket

sendmsg(2POSIX) -- REDE See send(2POSIX)

sendto(2POSIX) -- REDE See send(2POSIX)

sethostname(2POSIX) -- REDE See hostname(2POSIX)

sethostname(2POSIX) -- REDE See hostname(2POSIX)

setrlimit(2POSIX) -- REDE See getrlimit(2POSIX)

setsockopt(2POSIX) -- REDE See getsockopt(2POSIX)

settimeofday(2POSIX) --REDE See gettimeofday(2POSIX)

shm_open(2POSIX) -- PROCESSO open a shared memory object

shm_unlink(2POSIX) -- SISTE. E/S unlink a shared memory object

shutdown(2POSIX) -- PROCESSO shut down part of a full-duplex connection

socket(2POSIX) -- REDE create an endpoint for communication

socketpair(2POSIX) -- REDE create a pair of connected sockets

stat(2POSIX) -- ARQUIVO get file status

statfs(2POSIX) -- ARQUIVO get file system statistics

swapon(2POSIX) -- SISST. E/S add a swap device for swapping

symlink(2POSIX) -- ARQUIVO make a symbolic link to a file

sync(2POSIX) -- MEMORIA P. synchronize the disk block in-core status with that on disk

truncate(2POSIX) -- PROCESSO truncate a file to a specified length

umask(2POSIX) -- REDE set file creation mode mask

unlink(2POSIX) -- ARQUIVO remove a directory entry

unmount(2POSIX) -- SIST. E/S See mount(2POSIX)

utimes(2POSIX) -- CONTROLE DE ACESSO set file access and modification times

write(2POSIX) --SIST. E/S write output

writev(2POSIX) -- SIST. E/S See write(2POSIX)
